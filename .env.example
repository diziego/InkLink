NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional for future admin-only server actions and background jobs.
SUPABASE_SERVICE_ROLE_KEY=

# Stripe Checkout
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Temporary development-only provider fallback used until auth is wired.
DEV_PROVIDER_EMAIL=provider-demo@inklink.local

# Temporary development-only admin fallback used until auth is wired.
DEV_ADMIN_EMAIL=admin-demo@inklink.local
